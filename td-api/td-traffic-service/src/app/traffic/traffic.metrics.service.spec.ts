/*import { TrafficMetricsService } from './traffic.metrics.service';

describe('TrafficMetricsService', () => {
  it('returns a metrics snapshot with expected shape', () => {
    const service = new TrafficMetricsService();
    const snap = service.snapshot();

    expect(typeof snap.pid).toBe('number');
    expect(typeof snap.uptimeSec).toBe('number');
    expect(typeof snap.cpu.processPercent).toBe('number');
    expect(typeof snap.memory.rssBytes).toBe('number');
    expect(typeof snap.system.totalMemBytes).toBe('number');
    expect(typeof snap.eventLoop.utilizationPercent).toBe('number');
  });
});*/
